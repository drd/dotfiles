(global-set-key (kbd "M-1") 'next-buffer)
(global-set-key (kbd "M-2") 'previous-buffer)

(setq mac-command-modifier 'super) ; sets the Command key as Super
(setq mac-option-modifier 'meta) ; sets the Option key as Meta
